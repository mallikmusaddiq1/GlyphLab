var gridElement = $("symbolGrid"),
    scrollArea = $("scrollArea"),
    jumpHex = $("jumpHex"),
    jumpDec = $("jumpDec"),
    charInput = $("charInput"),
    hideCheckbox = $("hideUnrendered"),
    liveUpdateCheck = $("liveUpdateCheck"),
    draftArea = $("draftArea"),
    detailsModal = $("unifiedModal"),
    uniModalTitle = $("uniModalTitle"),
    detailsWrapper = $("detailsWrapper"),
    transformWrapper = $("transformWrapper"),
    detailsContent = $("detailsContent"),
    transformContent = $("transformContent"),
    tInput = $("tInput"),
    fontSearch = $("fontSearch"),
    filtersMenu = $("filtersMenu"),
    categorySearch = $("categorySearch"),
    shortcutsContainer = $("shortcutsContainer"),
    sidebar = $("sidebar"),
    panelToolSearch = $("panelToolSearch"),
    panelToolJump = $("panelToolJump"),
    panelToolDraft = $("panelToolDraft"),
    tabToolSearch = $("tabToolSearch"),
    tabToolJump = $("tabToolJump"),
    tabToolDraft = $("tabToolDraft"),
    btnSmartSearch = $("btnSmartSearch"),
    btnRawSearch = $("btnRawSearch"),
    defaultCardsToLoad = 40,
    currentTopHex = 33,
    currentBottomHex = 33,
    MAX_UNICODE = 1114111,
    visibilityCache = new Map,
    listData = [],
    cardPool = [],
    isFetching = !1,
    currentFetchId = 0,
    gridCols = 3,
    itemWidth = 130,
    itemHeight = 188.5,
    GAP = 12,
    canvas = document.createElement("canvas"),
    ctx = (canvas.width = 16, canvas.height = 16, canvas.getContext("2d", {
        willReadFrequently: !0
    })),
    emptyData = (ctx.font = "14px sans-serif", ctx.textBaseline = "top", ctx.clearRect(0, 0, 16, 16), new Uint32Array(ctx.getImageData(0, 0, 16, 16).data.buffer)),
    tofuData = (ctx.fillText(sFCP(1114111), 0, 0), new Uint32Array(ctx.getImageData(0, 0, 16, 16).data.buffer)),
    activeFilters = new Map,
    pendingFilterItems = [],
    isFilterMode = !1,
    isJumping = !1,
    hlWord = !1,
    fTO, rTO, currentDetailExportData = null,
    bookmarks = new Set,
    currentBkmTab = "SINGLE",
    currentFilterTab = "SINGLE",
    COMBINED_GROUPS = {},
    COMBINED_CAT_MAP = {},
    unicodeNames = {},
    unicodeDetailsData = {},
    U_BLOCKS = {},
    COMBINED_CHARS = {},
    fontCategories = {},
    APP_S = {},
    ast, isClearing = !1,
    wrapSearchCheck = $("wrapSearchCheck"),
    currentSearchMode = localStorage.getItem("glyphlab_search_mode") || "smart";